import { Loading } from 'ionic-angular';

import { LoadingModule } from './loading.module';
import { LoadingController } from './loading.controller';

export { LoadingModule, LoadingController, Loading };
