var fs = require('fs');

/**
 *

module.exports = function() {
    // restore original config file (changed by before_prepare_config.js)
    fs.rename('original.config.xml', 'config.xml', function (err) {
        if (err) throw err;
        console.log('renamed complete');
    });
};
 */